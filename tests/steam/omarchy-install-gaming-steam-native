#!/bin/bash

set -euo pipefail

echo "Starting native Steam..."
runuser -u omarchy -- env \
  XDG_RUNTIME_DIR="/run/user/1000" \
  DBUS_SESSION_BUS_ADDRESS="unix:path=/run/user/1000/bus" \
  steam >/dev/null 2>&1 &

